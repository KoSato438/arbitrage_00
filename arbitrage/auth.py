def auth():
    # Coincheck キーを定義
    access_key = 'access_key'
    secret_key = 'secret_key'

    # GMO キーを定義
    apiKey    = 'apikey'
    secretKey = 'secretkey'

    # Liquidキーを定義
    secret = "secret"
    token = "token"

    # 閾値
    thr=40
    mergin=5

    return [access_key,secret_key,apiKey,secretKey,secret,token,thr,mergin]